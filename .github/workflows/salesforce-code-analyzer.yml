name: Salesforce Code Analyzer

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  scan-code:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Salesforce Code Analyzer
        uses: forcedotcom/run-code-analyzer@v2
        with:
          # WRITTEN AS ONE LINE TO PREVENT YAML ERRORS
          run-arguments: --target ./force-app/main --format sarif --outfile CodeAnalyzerResults.sarif --normalize-severity --verbose

      - name: Upload Scan Results Artifact
        if: always() # Run this even if the scan found violations
        uses: actions/upload-artifact@v4
        with:
          name: salesforce-code-analyzer-results
          path: CodeAnalyzerResults.sarif

      - name: Upload SARIF to GitHub
        if: success() # Only upload to Security tab if file exists
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: CodeAnalyzerResults.sarif
          category: salesforce-code-analyzer
